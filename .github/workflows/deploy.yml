name: Deploy to Uberspace

on:
  workflow_run:
    workflows: ['Run Playwright Tests']
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Uberspace
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22.20.0

      - name: Install dependencies
        run: npm ci

      # adapter-node: erzeugt unter build/ einen eigenständigen Node-Server. Der wird
      # ausgeliefert und mit `node build` gestartet – kein `vite dev`/`npm install` mehr
      # bei jedem Restart.
      - name: Build
        run: npm run build

      - name: rsync deployments
        uses: burnett01/rsync-deployments@7.1.0
        with:
          # node_modules NICHT mitschicken (plattform-/ABI-abhängige native Module wie
          # sqlite3/mariadb werden auf dem Host installiert). build/ wird jetzt mitgeliefert.
          # --exclude bewahrt die ausgeschlossenen Pfade auf dem Host vor --delete.
          switches: -avzr --delete --exclude=".env" --exclude="node_modules"
          path: /
          # NICHT ~/html (= Apache-Docroot). Dort liegt das "website"-Projekt, dessen rsync
          # mit --delete und ohne excludes arbeitet – es hat diese App am 2026-08-11 samt
          # .env geloescht (Ausfall v0.7.37). Die App wird ohnehin von Node auf Port 5173
          # ausgeliefert, nicht von Apache, und gehoert deshalb nicht in den Docroot;
          # nebenbei war so der komplette Quellbaum oeffentlich lesbar.
          remote_path: /home/${{ secrets.UBERSPACE_USER }}/festival-app/
          remote_host: ${{ secrets.UBERSPACE_HOST }}
          remote_user: ${{ secrets.UBERSPACE_USER }}
          remote_key: ${{ secrets.DEPLOY_KEY_PRIVATE }}

      # Prod-Abhängigkeiten installieren (läuft, während der alte Service noch läuft →
      # keine Downtime), dann Service neu starten. `node build` startet in Sekunden, daher
      # reicht ein kurzes Health-Check-Fenster. Reused dieselben Secrets wie der rsync-Step.
      - name: Install prod deps, restart & health check
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.UBERSPACE_HOST }}
          username: ${{ secrets.UBERSPACE_USER }}
          key: ${{ secrets.DEPLOY_KEY_PRIVATE }}
          script: |
            cd "$HOME/festival-app" || exit 1
            # Die .env liegt nur auf dem Host (gitignored, per --exclude vor --delete
            # geschuetzt) – fehlt sie, startet `node --env-file=.env build` gar nicht erst.
            # Vor dem Restart pruefen, damit der Grund sofort dasteht statt erst nach zwei
            # Minuten Health-Check-Timeout (genau dieser Fall in v0.7.37).
            if [ ! -f .env ]; then
              echo "FEHLER: $PWD/.env fehlt. Ohne sie startet der Service nicht."
              echo "Anlegen mit MARIA_DB_USER/MARIA_DB_PASSWORD/MARIA_DB_NAME, dann Deploy erneut starten."
              exit 1
            fi
            # Nur Produktions-Abhängigkeiten (dependencies). adapter-node hält diese extern,
            # sie müssen also im node_modules des Hosts liegen.
            npm ci --omit=dev
            # Supervisor-Service "festival" führt `npm run start-server` (= PORT=5173
            # node build) im Verzeichnis ~/festival-app aus (~/etc/services.d/festival.ini).
            supervisorctl restart festival
            # Readiness statt blosser Erreichbarkeit: /api/health setzt ein SELECT 1 ab und
            # meldet ausstehende Migrationen (503, falls die DB nicht nutzbar ist). Ein
            # curl gegen / wuerde auch bei toter DB 200 liefern - diese Route fasst ohne
            # Session-Cookie keine Datenbank an. Genau so blieb der Ausfall in v0.7.25
            # unbemerkt. --fail laesst curl bei 503 mit Exit != 0 abbrechen.
            for i in $(seq 1 40); do
              if response=$(curl -fsS http://localhost:5173/festival/api/health); then
                echo "Service ist bereit (nach ~$((i * 3))s): $response"
                exit 0
              fi
              sleep 3
            done
            echo "Service wurde nach dem Restart nicht bereit (/api/health lieferte kein OK)."
            echo "Letzte Antwort: $(curl -sS -o - -w ' [HTTP %{http_code}]' http://localhost:5173/festival/api/health || echo 'keine Verbindung')"
            echo "Details stehen im Log des Supervisor-Service: supervisorctl tail -100 festival"
            exit 1
