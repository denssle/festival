# TODO

Hier werden alle offenen Aufgaben für das Projekt gesammelt.
Sortiert nach **Aufwand** (S → M → L), innerhalb jeder Stufe nach **Priorität** (🔴 → 🟡 → 🟢).

## Legende

| Priorität  | Bedeutung                                                         |
| ---------- | ----------------------------------------------------------------- |
| 🔴 Hoch    | Sicherheitsrisiko oder starke Beeinträchtigung der Nutzbarkeit    |
| 🟡 Mittel  | Wichtig für Qualität/Stabilität, aber kein akuter Handlungsbedarf |
| 🟢 Niedrig | Nice-to-have, technische Schulden oder Komfort-Features           |

| Aufwand | Bedeutung                                                  |
| ------- | ---------------------------------------------------------- |
| S       | Klein – wenige Zeilen, isolierte Änderung (< 1h)           |
| M       | Mittel – mehrere Dateien oder moderate Logik (1–4h)        |
| L       | Groß – Architekturänderung, viele Stellen betroffen (> 4h) |

---

## [S] Klein (< 1h)

- [x] 🔴 **Prod-Ausfall: zwei Projekte teilten sich `~/html`:** ~~`festival` und das `website`-Projekt deployten beide per rsync in den Apache-Docroot `~/html`; der `website`-Workflow nutzt `--delete` **ohne jedes `--exclude`**. Am 2026-08-11 räumte dessen Deploy (14:45 Hostzeit) das Verzeichnis leer – `build/`, `package.json`, `node_modules` und die `.env`. `festival` lief zunächst weiter (Code im Speicher) und starb erst um 15:47 beim ersten Request, der ein Asset frisch von der Platte brauchte (`ENOENT … 11.BlJCvzp9.js.br`, unbehandeltes `error`-Event). Die Neustarts scheiterten an der fehlenden `package.json`, nach dem nächsten Deploy an der `.env` (Node bricht mit Exit 9 ab, bevor irgendein Modul lädt). Die `.env` war unwiederbringlich weg – gitignored, per `--exclude` geschützt und ohne Backup.~~ _Erledigt (v0.7.38): Deploy-Ziel auf `~/festival-app` umgestellt (`remote_path` + Arbeitsverzeichnis im SSH-Step + `directory=` in `~/etc/services.d/festival.ini`). Die App wird von Node auf Port 5173 ausgeliefert und hat im Docroot nichts verloren; nebenbei war dort der komplette Quellbaum öffentlich lesbar (`/package.json`, `/src/hooks.server.ts`, `/TODO.md`, `/CLAUDE.md` – jeweils HTTP 200). Zusätzlich prüft `deploy.yml` die `.env` jetzt **vor** dem Restart, damit der Grund sofort im Log steht statt erst nach zwei Minuten Health-Check-Timeout._ _(Deployment)_
- [x] 🔴 **Deploy-Health-Check war blind:** ~~Der Check lief per `curl -fsS` gegen `/`. Diese Route fasst ohne Session-Cookie keine DB an (`getCurrentUserBySessionToken` steigt bei fehlendem Token vor der ersten Query aus) und antwortet mit 303 auf `/login` – was `curl --fail` als Erfolg wertet. Der Deploy war dadurch selbst bei toter Datenbank grün; genau so blieb der `.env`-Ausfall unbemerkt.~~ _Erledigt (v0.7.26): Readiness-Endpunkt `GET /api/health` (`SELECT 1` + Anzahl ausstehender Migrationen, 503 im Fehlerfall, `no-store`), im Hook vor der Session-Auflösung durchgereicht, damit er auch bei toter DB antwortet. `deploy.yml` prüft jetzt diesen Endpunkt. Abgesichert durch `tests/health.spec.ts`._ _(Deployment)_
- [x] 🔴 **Credential-Guard brach den Build ab:** ~~Der Guard aus v0.7.25 stand auf Modulebene in `sequelize.ts`. `vite build` importiert die Servermodule in seiner Analyse-Phase (`analyse.js`) und führt damit Top-Level-Code aus – in der CI, die keine `MARIA_DB_*`-Variablen kennt, schlug der Build dadurch fehl.~~ _Erledigt (v0.7.27): als `assertDatabaseCredentials()` nach `startDB()` verschoben, wo die Verbindung tatsächlich aufgebaut wird. Lokal nicht aufgefallen, weil die Dev-`.env` über `MARIA_DB_NAME="dev"` in den SQLite-Zweig läuft – ein Build ohne `.env` deckt solche Fälle auf._ _(Deployment)_
- [x] 🔴 **Baseline lief gegen bestehende Prod-DB:** ~~Die Produktions-DB stammt aus der `sync({ alter: true })`-Zeit und hat kein `SequelizeMeta`. umzug hielt `0001-initial-schema` deshalb für ausstehend: Die `CREATE TABLE IF NOT EXISTS` liefen folgenlos durch, aber `ADD UNIQUE INDEX` kennt kein IF NOT EXISTS und brach mit `Duplicate key name` (Fehler 1061) ab – der Serverstart scheiterte (fail-fast).~~ _Erledigt (v0.7.28): `stampBaselineIfLegacySchema()` trägt die Baseline als ausgeführt ein, ohne sie auszuführen – aber nur, wenn noch keine Migration protokolliert ist UND die Tabelle `users` bereits existiert. Leere DBs laufen weiter regulär durch alle Migrationen. Abgesichert durch `baseline.spec.ts` (3 Fälle)._ _(Datenbank)_
- [x] 🟡 **CI testet den Produktionspfad nie:** ~~Die Pipeline fährt `PLAYWRIGHT=true` gegen In-Memory-SQLite – weder `node build` noch der MariaDB-Zweig von `startDB()` werden je ausgeführt. Alle drei Ausfälle aus v0.7.25–v0.7.28 wären in einem Smoke-Test des gebauten Artefakts gegen einen MariaDB-Container sofort aufgefallen.~~ _Erledigt (v0.7.29): Job `smoke` in `tests.yml` + `scripts/smoke-test.sh`. Baut das Artefakt, installiert wie der Host nur Prod-Deps (`npm ci --omit=dev`, fängt falsch einsortierte Runtime-Dependencies) und startet es per `npm run start-server` gegen einen MariaDB-Service-Container. Zwei Szenarien: frische DB und Bestands-DB ohne `SequelizeMeta`. Prüft `dialect: 'mariadb'` explizit, damit ein stiller SQLite-Rückfall nicht grün meldet. Blockiert den Deploy, da `deploy.yml` am Gesamtergebnis hängt._ _(Testen)_
- [x] 🔴 **Prod-Crash: `.env` wurde nicht geladen:** ~~`node build` (adapter-node) liest – anders als das frühere `vite dev` – keine `.env`; `MARIA_DB_USER`/`MARIA_DB_PASSWORD` blieben `undefined`, der mariadb-Treiber verband sich als Benutzer `''` ohne Passwort und brach mit Fehler 1045 (`ER_ACCESS_DENIED_ERROR`) ab. Durch den fail-fast aus v0.7.24 wurde daraus ein Startabbruch statt eines stillen Fehlers.~~ _Erledigt (v0.7.25): `start-server` startet mit `node --env-file=.env build`; zusätzlich prüft `sequelize.ts` im MariaDB-Zweig die Zugangsdaten vorab und wirft eine sprechende Meldung._ _(Deployment)_
- [x] 🟡 **GitHub-Actions – Node-20-Deprecation:** ~~`actions/checkout@v4` und `actions/setup-node@v4` laufen noch auf Node 20.~~ _Erledigt (v0.7.10): `checkout@v5`, `setup-node@v5` und `upload-artifact@v5` (alle Node-24-nativ) in `tests.yml` und `deploy.yml`._ _(Deployment)_
- [x] 🟡 **Dokumentation:** ~~Produktions-Umgebung (env) sauber dokumentieren.~~ _Erledigt (v0.7.6): CLAUDE.md Abschnitt 6 um erforderliche Env-Variablen, die SQLite-Falle (`MARIA_DB_NAME != 'dev'` in Prod), Runtime/Port, Deploy-Secrets, Supervisor-Service und ungenutzte Redis-Variablen ergänzt; `pipeline.yml`-Referenz auf `tests.yml`/`deploy.yml` korrigiert._ _(Deployment)_
- [x] 🟢 **E-Mail-Eindeutigkeit beim Update:** ~~Die Profil-Update-Action prüft die Eindeutigkeit des Nicknames, aber nicht der E-Mail.~~ _Erledigt (v0.7.5): neue `UserService.emailTakenByOtherUser(email, userId)` (erlaubt die eigene unveränderte E-Mail, blockt fremde) wird in der Update-Action vor `updateUser` geprüft._ _(Datenschutz)_
- [x] 🟢 **Fremdschlüssel-Casing:** ~~Der Workaround `event.UserId || (event as any).userId` deutet auf uneinheitliches FK-Casing hin.~~ _Erledigt (v0.7.5): Modelle definieren `UserId` explizit (Triad korrekt), die `as any`-Krücke an allen 5 Stellen entfernt (festivalEvent.attributes.ts 2×, festival-event.service.ts 2×, guest-information.service.ts 1×)._ _(Datenbank)_
- [x] 🟢 **API-Body vereinheitlichen:** ~~`join` liest `request.json()`, `cancel-invitation` dagegen `request.blob()` → `.text()`.~~ _Erledigt (v0.7.5): `cancel-invitation` nutzt jetzt JSON (`{ comment }`), Server liest `request.json()`, Client sendet `JSON.stringify({ comment })` – einheitlich mit `join`._ _(Datenbank)_
- [x] 🟢 **`alert()` ersetzen:** ~~Im Join-Fehlerpfad wird noch natives `alert()` genutzt.~~ _Erledigt (v0.7.5): beide `alert()`-Aufrufe (Fehler- und Netzwerkfehler-Pfad) durch den vorhandenen `InfoDialog` ersetzt._ _(UX)_
- [x] 🟢 **Bild-Store-Cache:** ~~`userImage.store.ts` hält eine modulweite `Map`, die nie geleert wird.~~ _Erledigt (v0.7.7): in einen beschränkten LRU-Cache umgebaut (`MAX_CACHED_IMAGES = 50`; zuletzt genutzter Eintrag wird neu einsortiert, überzählige älteste verworfen)._ _(Performance)_
- [x] 🟢 **Dialog-Vorbefüllung entkoppeln:** ~~Ein `$effect` synchronisiert laufend die Zu-/Absage-Dialogfelder aus abgeleiteten Gastdaten.~~ _Erledigt (v0.7.7): Dauer-`$effect` entfernt; die Felder werden jetzt einmalig beim Öffnen in `joinFestival`/`cancelInvitation` aus den aktuellen Gastdaten gesetzt (keine Überschreib-Race mehr)._ _(UX)_
- [x] 🟡 **`npm run lint` ist kaputt:** ~~ESLint 9 findet keine `eslint.config.js` (Flat Config) und bricht ab; zusätzlich ignoriert es die veraltete `.eslintignore`. Auch `prettier --check` schlägt fehl.~~ _Erledigt (v0.7.15): `.eslintrc.cjs` + `.eslintignore` durch `eslint.config.js` (Flat Config) ersetzt (Regeln 1:1 übernommen, Ignores in `ignores`); dafür `@eslint/js@^9`, `typescript-eslint@^8`, `globals@^16` ergänzt (`@eslint/js@10` verlangt ESLint 10 → auf 9er-Linie gepinnt). `npm run format` einmal durchlaufen. `npm run lint` ist grün (0 Errors); Rest sind Warnungen (`no-explicit-any`). Leeres `interface FrontendUser extends BaseUser {}` → Type-Alias (seit v0.7.12 feldgleich)._ _Nachtrag (v0.7.16): `npm run lint` war nur in der Linux-CI grün, auf Windows-Arbeitskopien schlug `prettier --check` fehl – Git checkt wegen `core.autocrlf=true` + `.gitattributes` (`* text=auto`) mit CRLF aus, Prettier erwartet LF. Gefixt mit `"endOfLine": "auto"` in `.prettierrc` (im Repo bleibt alles LF, nur die Arbeitskopie darf CRLF sein); zusätzlich `/.claude` in `.prettierignore` (lokale Tooling-Config, nicht im Repo)._ _(Testen)_
- [x] 🟡 **CLAUDE.md – falsche CI-Aussage:** ~~Abschnitt 1 behauptet „Die Pipeline nutzt einen MariaDB Service Container". Stimmt nicht: `tests.yml` hat keinen Service-Container, die CI fährt wie lokal auf In-Memory-SQLite (`PLAYWRIGHT=true`).~~ _Erledigt (v0.7.16): Aussage in CLAUDE.md korrigiert und um die Konsequenz ergänzt (MariaDB-spezifisches SQL wird in KEINER automatisierten Prüfung getestet). Der echte MariaDB-Job kommt als Drift-Schutz mit der Migrationsstrategie (siehe [L] Schema-Stabilität, Schritt 5)._ _(Deployment)_
- [x] 🟡 **`any` in `src` typisieren:** ~~Rund 40 `any` im Produktionscode, fast alle das zweite Generic von `Model<TAttributes, TCreationAttributes>` – d. h. der Typ, den `.create()` erwartet, war komplett ungeprüft.~~ _Erledigt (v0.7.17): pro Modell ein abgeleiteter `*CreationAttributes`-Typ via Sequelize' `Optional<T, K>` (optional sind Zeitstempel, `allowNull`-Spalten und eager-geladene Assoziationen). Alle 10 Modelle, die Services und die Attribut-Typen umgestellt; `src` ist jetzt `any`-frei (Typecheck grün). Der Typechecker deckte dabei drei echte Fehler auf – siehe unten._ _(Testen)_
- [x] 🟡 **`any` in `tests/` typisieren:** ~~In den Playwright-Specs stehen noch 21 `any` (Helper wie `(page: any)`).~~ _Erledigt (v0.7.18): durch die echten Playwright-Typen ersetzt (`Page`, `BrowserContext`, `Response` in den `waitForResponse`-Callbacks). **`@typescript-eslint/no-explicit-any` steht jetzt auf `error`** – das gesamte Repo (`src` + `tests`) ist `any`-frei, neue `any` lassen den Lint fehlschlagen._ _(Testen)_
- [x] 🟢 **`svelte/no-navigation-without-resolve`:** ~~Die Regel (neu in eslint-plugin-svelte 3) verlangt `resolve()` um jede `href`-/`goto()`-Ziel-URL und schlägt an ~37 Stellen an.~~ _Erledigt (v0.7.20): alle 37 Stellen in 19 Dateien auf `resolve()` aus `$app/paths` umgestellt (verfügbar seit Kit 2.26). Statische Pfade als `resolve('/login')`, **dynamische Routen als Route-ID + Parameter** (`resolve('/user/[user_id]', { user_id: id })`) – dadurch prüft TypeScript Routen und Parameter. Regel steht jetzt auf `error`. **`npm run lint` ist damit erstmals komplett grün: 0 Errors, 0 Warnungen.** Im Browser gegengeprüft (Header-, Footer- und dynamische Links geklickt): `resolve()` erzeugt beim SSR relative Pfade (`../user/<id>`) – genau der Grund für die Regel, denn mit konfiguriertem `base`-Pfad wären die hartcodierten absoluten Links gebrochen._ _(Testen)_

- [x] 🟢 **404-Rauschen bei Avataren ohne Bild:** ~~`GET /user-image/<id>` antwortete mit **404**, wenn ein Nutzer kein Profilbild hinterlegt hat. Der Client fing das zwar über das Fallback-Bild ab, aber jeder bildlose Avatar erzeugte einen Fehler-Request in der Browser-Konsole.~~ _Erledigt (v0.7.21): Endpoint antwortet jetzt mit **204 No Content** – kein Bild zu haben ist der Normalfall, kein Fehler. **Falle dabei:** `response.ok` ist bei 204 ebenfalls `true`, der Body aber leer; ohne explizite 204-Prüfung im Store hätte ein leerer String als Bild gesetzt und der Spinner wäre endlos gelaufen. Im Browser verifiziert: Fallback-Bild erscheint, kein Spinner, keine 4xx-Requests mehr._ _(UX)_

### Aus Code-Review (2026-07-31)

- [x] 🔴 **`MIN_PASSWORD_LENGTH = 3` anheben:** ~~Drei Zeichen sind praktisch keine Passwort-Anforderung.~~ _Erledigt (2026-07-31): Konstante in `src/lib/constants.ts` auf 8 gesetzt (greift in Registrierungs-Action + Passwortänderung); die hartcodierten `minlength="3"` der Passwortfelder in `registration/+page.svelte` durch die Konstante ersetzt. Bestandspasswörter bleiben gültig. Die vollständige Richtlinie (Komplexität, Wiederherstellung) bleibt beim [L]-Punkt „Passwort-Management"._ _(Sicherheit)_
- [x] 🔴 **Bild-Upload vs. `BODY_SIZE_LIMIT`:** ~~`image.logic.ts` erlaubt 1 MB Bilder, als Base64-Data-URI ~1,37 MB Request-Body. adapter-node lehnt Bodies über 512 KB (Default `BODY_SIZE_LIMIT`) ab → in Prod scheitern Uploads über ~370 KB Bilddatei am Adapter, bevor die eigene Validierung greift.~~ _Erledigt (2026-07-31): `BODY_SIZE_LIMIT=2M` im `start-server`-Script (`package.json`) gesetzt – das Script läuft auf dem Prod-Host und wird mit deployt, keine Supervisor-Config-Änderung nötig. Noch offen zur Verifikation: einmal mit einem ~1-MB-Bild gegen den Prod-Build (`npm run build && npm run start-server`) gegentesten._ _(Deployment)_
- [x] 🟡 **`startDB()` fail-fast:** ~~Der `catch` in `src/lib/db/db.ts` loggt Verbindungsfehler nur – die App startet „erfolgreich" ohne DB.~~ _Erledigt (v0.7.24) im Zuge der Migrationsumstellung: `startDB()` wirft jetzt bei DB-/Migrationsfehlern; der Fehler propagiert durchs Top-Level-await in `hooks.server.ts`, der Prozess startet nicht (Supervisor + Health-Check machen es sichtbar)._ Ursprünglicher Befund: Der `catch` loggt Verbindungsfehler nur – die App startet „erfolgreich" ohne DB und wirft dann auf jedem Request. Gerade wegen der SQLite-Falle (CLAUDE.md §6) sollte ein DB-Fehler in Produktion den Prozess beenden (`process.exit(1)` bzw. Fehler durchwerfen); Supervisor-Restart + Health-Check im Deploy fangen das sauber auf. _(Deployment)_
- [x] 🟡 **Nickname-Schutz beim Profil-Update:** ~~Ein Nickname aus reinen Leerzeichen passiert `nickNameInvalid` ungehindert (weder leer noch vergeben) und würde gespeichert – der Login läuft über den Nickname, der Zugang wäre damit weg.~~ _Erledigt (v0.7.35): `updateUser` lehnt Nicknames ohne sichtbare Zeichen mit `'Data Missing'` (→ 422) ab. Bewusst im Service statt in der Action, damit der Schutz unabhängig vom Aufrufer greift. Abgedeckt durch `user.service.spec.ts` (gegen In-Memory-SQLite; per Gegenprobe verifiziert, dass die Tests ohne den Guard fehlschlagen)._ _(Sicherheit)_
- [x] 🟡 **`readFormDataFrontEndUser` erzeugt `"null"`-Strings:** ~~`String(values.get('email'))` macht aus fehlenden Formularfeldern den String `"null"`, der als E-Mail/Vorname/Nachname in der DB landet (`user.service.ts`).~~ _Erledigt (v0.7.34): neue reine Funktion `readTextField()` in `user.logic.ts` prüft auf `typeof === 'string'` und liefert sonst `''` – deckt auch `File`-Werte ab (ergaben zuvor `"[object File]"`). Nebeneffekt: `emailTakenByOtherUser` bekommt jetzt `''` statt `"null"` und steigt dadurch früh aus – vorher blockierten sich zwei Nutzer ohne E-Mail gegenseitig beim Speichern. Abgedeckt durch 5 Tests in `user.logic.spec.ts`._ _(Datenbank)_
- [x] 🟢 **Comment-Controller: korrekte Statuscodes:** ~~`comment.controller.ts` antwortet bei fehlender Session bzw. fehlenden Daten mit 500 statt 401/400.~~ _Erledigt (2026-07-31) im Zuge des Session-Umbaus: alle vier Handler antworten jetzt 401 ohne Session und 400 bei fehlenden Daten._ _(UX)_
- [ ] 🟢 **Kommentar-Ziel validieren:** `POSTComment` prüft nicht, ob `pathId` (Festival- oder User-ID) existiert – man kann Kommentare an beliebige IDs schreiben (Waisen-Datensätze, `writtenTo` hat als polymorphe Spalte keinen FK). Vor dem Speichern Existenz von Festival/User prüfen. _(Datenbank)_
- [x] 🟢 **bcrypt asynchron:** ~~`hashSync`/`compareSync` blockieren den Event-Loop des einzigen Node-Prozesses ~100 ms pro Login/Registrierung/Passwortänderung.~~ _Erledigt (v0.7.37): `saltPassword` gibt jetzt ein `Promise<string>` zurück (`hash(password, 10)` – erzeugt das Salt selbst, `genSaltSync` entfällt), `loginWithCredentials` nutzt `await compare(…)`. Aufrufer angepasst (`register`, `updatePassword`). bcrypt-ts arbeitet in den async-Varianten nicht über einen Thread-Pool, sondern stückelt die Runden über `setImmediate` – die einzelne Operation dauert minimal länger, der Prozess bleibt aber während der Passwortprüfung ansprechbar._ _(Performance)_
- [x] 🟢 **Token-Vergleich timing-safe:** ~~`validateSessionToken` vergleicht Tokens mit `!==`.~~ _Obsolet (2026-07-31): Durch den Session-Umbau gibt es keinen String-Vergleich im Code mehr – der Token wird per `findOne({ where: { token } })` direkt in der DB nachgeschlagen._ _(Sicherheit)_
- [x] 🟢 **Cache-Header für Avatare:** ~~`GET /user-image/[user_id]` liefert Bilder ohne Caching-Header – jede Profil-/Listenseite lädt alle Avatare komplett neu (der clientseitige LRU-Store hilft nur innerhalb einer Session).~~ _Erledigt (v0.7.37): `Cache-Control: private, max-age=60, must-revalidate` plus inhaltsbasierter `ETag` (SHA-1 über die Bilddaten); bei passendem `If-None-Match` antwortet der Endpoint mit 304 ohne Body. `private`, weil die Antwort eine Session voraussetzt und nicht in gemeinsame Caches (nginx) gehört. Die 204-Antwort (Nutzer ohne Bild – der häufigste Fall) bekommt denselben Header. **Falle dabei:** Nach einem Upload lud `AvatarUpload` das Bild sofort neu und hätte im `max-age`-Fenster das ALTE Bild aus dem Browser-Cache bekommen – `loadUserImage(userId, true)` setzt dafür `cache: 'reload'`. Abgedeckt durch einen E2E-Test in `settings.spec.ts` (204 cachebar, 200 mit ETag, 304 bei passendem, 200 bei fremdem ETag). Grundsätzliche Ablage (Dateisystem statt DB) bleibt beim [L]-Punkt „Bilder in der Datenbank". _(Performance)\_
- [x] 🟢 **`sqlite3` aus den Prod-Dependencies:** _Erledigt (v0.7.24): `sqlite3` nach `devDependencies` verschoben, `sequelize-cli` komplett entfernt (umzug braucht es nicht); dafür `umzug` als Prod-Dependency ergänzt (läuft beim Serverstart)._ Ursprünglicher Befund: `sqlite3` steht in `dependencies` und wird per `npm ci --omit=dev` auf dem Prod-Host mitinstalliert – ein natives Modul, das dort nie genutzt wird (Prod = MariaDB) und nur Installationszeit/Bruchrisiko kostet. Nach `devDependencies` verschieben (CI/lokal brauchen es weiterhin). `sequelize-cli` analog – bereits als Schritt 7 im [L]-Migrationsplan erfasst, kann aber unabhängig davon sofort passieren. _(Deployment)_

## [M] Mittel (1–4h)

- [x] 🔴 **Cookie-Sicherheit (Flags):** ~~Verwendung von `HttpOnly`, `Secure` und `SameSite=Strict` Flags.~~ _Erledigt (v0.7.3): in `createSessionCookie` explizit `httpOnly: true`, `sameSite: 'strict'`, `secure: !dev` (an Build-Modus gekoppelt wegen localhost-E2E + Reverse-Proxy in Prod)._ Signierung siehe „Hook – JWT/HMAC" unten (aktuell durch DB-validierten Zufalls-Token abgesichert). _(Sicherheit)_
- [x] 🔴 **Access-Control:** ~~Kritische Aktionen und Routen serverseitig auf fehlende Zugriffsberechtigungen prüfen.~~ _Verifiziert (2026-07-09): alle mutierenden Flächen abgesichert – Festival löschen/bearbeiten (Owner via `isChangeAllowed`), join/cancel (self-scoped auf `user.id`), Gruppen löschen/verlassen (Owner/Self), Kommentar löschen/ändern (Autor `writtenBy`), Freundesanfrage annehmen (nur Empfänger). Kein Code-Change nötig. Verbleibende verwandte Punkte separat erfasst: Festival-Sichtbarkeit (unten, L) und `FrontendUser`-E-Mail-Leak (unten, M)._ _(Sicherheit)_
- [x] 🔴 **Session-Timeout:** ~~Einführung einer zeitlichen Begrenzung für Session-Token, um das Risiko von Session-Hijacking zu minimieren.~~ _Erledigt (v0.7.2): absolute Token-Lebensdauer `SESSION_MAX_AGE_MS` (30 Tage), serverseitige Prüfung via `isSessionTokenExpired` in `validateSessionToken`; abgelaufenes Token wird über den Auth-Hook ausgeloggt (DB-Cleanup)._ _(Sicherheit)_
- [x] 🔴 **Event-Logik:** ~~Sicherstellen, dass alle kritischen Aktionen (Teilnahme, etc.) autorisiert sind.~~ _Verifiziert (2026-07-09) im Zuge des Access-Control-Audits: Teilnahme (join/cancel) ist self-scoped, Bearbeiten/Löschen owner-gebunden. Details siehe Access-Control-Eintrag._ _(Festivals)_
- [x] 🔴 **N+1 Queries – Festival-Ersteller:** ~~`mapToFrontendFestivalEvent` lädt den Festival-Ersteller per separatem `UserService.loadFrontEndUserById()`-Call.~~ _Erledigt (v0.7.4): `getAllFestivals` lädt den Ersteller per Eager Loading (`include: { model: User, as: 'User' }`); `mapToFrontendFestivalEvent` liest ihn aus `event.User` (Fallback auf Einzel-Load nur noch bei `createFestival`, wo kein Include existiert)._ _(Performance)_
- [x] 🟡 **Login-Schutz:** ~~Implementierung von Rate-Limiting / Brute-Force-Schutz.~~ _Erledigt (v0.7.14): Sliding-Window-Limiter in `rate-limit.logic.ts` (pur, unit-getestet; 5 Fehlversuche pro IP+Nickname in 15 min, Konstanten in `constants.ts`), prozessweiter Singleton in `login-rate-limit.ts`; Login-Action prüft die Sperre VOR der Passwortprüfung, setzt bei Erfolg zurück. In-Memory reicht (ein Node-Prozess); hinter Proxy ohne ADDRESS_HEADER degradiert der Schutz zu "pro Nickname". E2E-Lockout-Test in authentication.spec._ _(Sicherheit)_
- [x] 🟡 **Passwortänderung härten:** ~~Die Änderung unter `/settings` verlangt weder das aktuelle Passwort noch ein Wiederholungsfeld.~~ _Erledigt (v0.7.11): Action verlangt `currentPassword` (Prüfung via `loginWithCredentials`) + `passwordRepeat` (Formvalidierung in `validatePasswordChange`, user.logic.ts, unit-getestet); nach Erfolg wird das Session-Token rotiert (`createSessionCookie` mit `forceNewToken`). E2E: Happy Path angepasst + Negativtest (falsches aktuelles Passwort)._ _(Sicherheit)_
- [x] 🟡 **`FrontendUser` leakt E-Mail:** ~~Das `FrontendUser`-Modell enthält `email` und wird an vielen Stellen an den Client serialisiert.~~ _Erledigt (v0.7.12): `email` aus `BaseUser`/`FrontendUser` entfernt (nur noch `BackendUser`, serverseitig) → alle eingebetteten Vorkommen (FriendRequestData, FrontendComment, Freundeslisten …) sind automatisch dicht. Eigenes Profil erhält die E-Mail separat über `UserTransferData.email` (`UserService.getEmailById`, nur bei `isOwnProfil`); `UserDataReadOnly` zeigt keine E-Mail mehr an._ _(Datenschutz)_
- [x] 🟡 **Beziehungen:** ~~Many-to-Many Implementierungen (besonders `Friendship`) und Kaskadenlöschungen verifizieren.~~ _Erledigt (v0.7.15): Schema gegen die echte DB verifiziert – alle FKs stehen auf `ON DELETE CASCADE` (inkl. **beider** Spalten von `friendships` und `groupMembers`), Unique-Indizes auf `(FestivalEventId, UserId)` und `(GroupId, UserId)` vorhanden. Zwei echte Lücken gefixt: (1) `comments.writtenTo` ist polymorph (Festival- ODER User-ID) und kann keinen FK haben → beim Festival-Löschen blieben Kommentare als Waisen zurück; `CommentService.deleteCommentsWrittenTo()` wird jetzt in `deleteFestival` aufgerufen. (2) `addFriend` prüft jetzt `areFriends`/Selbst-Freundschaft, da der Unique-Index die gespiegelte Zeile `(friend2Id, friend1Id)` nicht abdeckt. Neuer Integrationstest `src/lib/db/cascade.spec.ts` (4 Fälle, gegen In-Memory-SQLite). Offen: Profil-Kommentare (`writtenTo` = User-ID) müssen beim Account-Löschen mitentfernt werden – gehört ins Löschkonzept (siehe [L] Datenschutz), da es aktuell keine Account-Löschung gibt. \_Nachtrag (2026-07-31): Die damals „gegen die echte DB" verifizierten Unique-Indizes auf `groupMembers` und `friendships` stehen NICHT in den Modell-Definitionen (nur `guestInformations` definiert einen) – sie existieren also höchstens in der alten Prod-DB, nicht in frisch aufgebauten DBs. Siehe den offenen Punkt „Unique-Constraints auf DB-Ebene" unter [M]._ \_(Datenbank)\_
- [x] 🟡 **N+1 Queries – Freundesliste:** ~~`getFriendList` und `convertToFriendRequest` laden User-Daten einzeln pro Eintrag.~~ _Erledigt (v0.7.13): `getFriendList` lädt alle Freunde mit EINEM Query (`UserService.loadFrontendUsersByIds`, WHERE id IN …); `getReceived-/getSentFriendRequests` laden `sender`/`receiver` per `include` mit, `convertToFriendRequest` liest bevorzugt die eager-geladenen Assoziationen (Fallback auf Einzel-Load ohne include)._ _(Performance)_
- [ ] 🟡 **Hook – JWT/HMAC:** Session-Token als signiertes JWT/HMAC implementieren, sodass kein DB-Lookup pro Request mehr nötig ist (statische Assets werden bereits übersprungen). _Entscheidung (2026-07-31): Beim Session-Umbau wurde bewusst das opake DB-Token gewählt (sofortiger Widerruf, kein Secret-Management); dieser Punkt bleibt als reine Performance-Option offen. Der Hook macht seither pro Request genau zwei Lookups (SessionToken + User) und schreibt den Cookie nicht mehr bei jedem Request neu._ _(Performance)_
- [ ] 🟡 **Backend-Logik:** Wichtige Geschäftslogik mit Unit- oder Integrationstests absichern. _Stand (2026-07-31): in weiten Teilen erledigt – `user.logic`, `image.logic`, `rate-limit.logic`, `festival-event.logic` sind unit-getestet, dazu `cascade.spec.ts` als Integrationstest. Ungetestet bleiben die Service-Klassen selbst (z. B. `FriendshipService`-Richtungslogik, `GroupService.leaveGroup`-Owner-Regel) – Punkt entsprechend eingrenzen oder schließen._ _(Testen)_
- [ ] 🟢 **E2E-Test-Isolation (Architektur):** Die Suite ist inzwischen zuverlässig grün (Hydration-Race-Flakiness behoben, siehe `openDialog`/`clickForResponse` in `test-utils.ts`). Weiterhin teilen sich alle Specs eine In-Memory-SQLite-DB (ein Dev-Server) und `comments.spec`/`settings.spec`/`friendship.spec` leeren sie global via `/api/test/reset`. Langfristig sauberer: pro Spec/Worker isolierte DB statt globalem Truncate. Beobachtete Rest-Flakes unter Suite-Last: verlorene Klicks vor Hydration (Logout inzwischen via `logout()`-Helper retry-fest, v0.7.12); einmalig „Gruppe bearbeiten“ (Name-Feld-Fill ging verloren, Beschreibung kam an) – bei Wiederauftreten das `openDialog`-Retry-Muster auf den Edit-Flow übertragen. _(Testen)_
- [ ] 🟡 **Error Handling:** Einheitliche Fehlermeldungen und klare Validierungs-Feedback im Frontend. _(UX)_
- [ ] 🟡 **Infrastruktur:** Backup-Konzept, HTTPS-Konfiguration und Start-/Restart-Verhalten im Zielsystem sicherstellen. _(Deployment)_
- [ ] 🟢 **Teilnehmer-Status:** Option "Vielleicht" für Teilnehmer anbieten. _(Festivals)_
- [ ] 🟢 **SessionToken:** Unterstützung mehrerer Sitzungen pro Benutzer auf DB-Ebene evaluieren. _(Datenbank)_
- [x] 🟢 **Edge Cases:** ~~Behandlung von leeren Zuständen.~~ _Erledigt (v0.7.19): Bestandsaufnahme aller Listen ergab, dass die meisten schon einen Leerzustand hatten (Gruppen, Gruppenmitglieder, Freundesliste, Freundschaftsanfragen, Zu-/Absagen, besuchte Festivals). Es fehlten genau zwei – und ausgerechnet die beiden prominentesten: die **Festival-Liste auf der Startseite** (neue Nutzer sahen unter „Neues Fest anlegen" schlicht nichts) und die **Kommentarliste** (`Comments.svelte`, auf Festival- und Profilseiten). Beide über den `{:else}`-Block von `{#each}` gelöst (kein zusätzlicher `length`-Check nötig), Wortlaut und Markup an die bestehenden Leerzustände angeglichen (schlichtes `<p>`, keine neue CSS-Klasse). Im Browser gegen eine frische DB verifiziert._ _(UX)_
- [ ] 🟢 **Monitoring:** Logging und Fehlertracking einrichten. _(Deployment)_
- [ ] 🟢 **`overrides` für `cookie`/`uuid` auflösen:** In `package.json` erzwingen zwei `overrides` gepatchte Transitiv-Versionen (`cookie → ^0.7.0` via `@sveltejs/kit`, `uuid → ^11.1.1` via `sequelize@6`), da es upstream keinen nicht-breaking Fix gibt. Sobald ein `@sveltejs/kit` mit `cookie ≥ 0.7` bzw. `sequelize 7` (nutzt `uuid` nativ, kein `^8` mehr) verfügbar ist, die zugehörigen Kernpakete anheben und die Overrides wieder entfernen. Danach `npm audit` + Build/Tests gegenprüfen. _Geprüft am 2026-07-13 (v0.7.16): **beide Overrides weiterhin nötig.** `@sveltejs/kit@2.69.2` (latest) hängt immer noch an `cookie@^0.6.0`; `sequelize@latest` ist 6.37.8 mit `uuid@^8.3.2` (Version 7 existiert nur als `alpha`: 7.0.0-alpha.9 – kein Produktions-Upgrade). Gegenprobe: ohne die Overrides meldet `npm audit` 5 Schwachstellen (3 low, 2 moderate), mit ihnen 0. Nächste Prüfung sinnvoll, wenn sequelize 7 stabil wird oder Kit die cookie-Range anhebt._ _(Datenbank)_

### Aus Code-Review (2026-07-31)

- [x] 🔴 **Unique-Constraints auf DB-Ebene:** _Erledigt (2026-07-31): `unique: true` auf `users.nickname`; Unique-Indizes auf `friendships(friend1Id, friend2Id)`, `groupMembers(GroupId, UserId)` und `friendRequests(senderId, receiverId)` in den Modell-Definitionen ergänzt (nur `guestInformations` hatte bereits einen). `UniqueConstraintError` wird abgefangen in `register` (→ null), `updateUser` (→ 'Failure'), `addFriend`/`createFriendRequest` (idempotent ignoriert) und `joinGroup` (→ 'Already in Group'). `users.email` bewusst OHNE Unique-Index (allowNull-Spalte, leere Strings möglich – Eindeutigkeit prüft weiterhin `emailTakenByOtherUser`). **Offen für Prod:** ~~`sync()` ergänzt Indizes nicht an bestehenden Tabellen.~~ \_Gelöst (v0.7.24): Die Baseline-Migration `0001-initial-schema` enthält alle Unique-Indizes; die Prod-Tabellen (verzichtbare Testdaten) werden vor dem nächsten Deploy gedroppt, die Migration legt alles frisch an – siehe Cutover-Hinweis beim Schema-Stabilität-Punkt._ Ursprünglicher Befund: `nickname` (und ggf. `email`) haben kein `unique: true` – Eindeutigkeit wird nur per check-then-create in der Anwendung geprüft (`nickNameInvalid` → `register`, `emailTakenByOtherUser`, `joinGroup`, `addFriend`, `createFriendRequest`). Zwei parallele Requests können denselben Nickname anlegen; da der Login per `findOne` über den Nickname läuft, führt ein Duplikat zu Account-Verwechslung. Verschärft durch die Cookie-Schwäche (siehe nächster Punkt): das Profil-Update prüft Eindeutigkeit nur bei `nicknameChanged` gegenüber dem **Cookie-Wert** – manipulierter Cookie umgeht die Prüfung komplett. Fix: `unique: true` auf `users.nickname` + Unique-Indizes auf `friendRequests(senderId, receiverId)`, `groupMembers(GroupId, UserId)` und `friendships(friend1Id, friend2Id)` und die Unique-Verletzung beim Create/Update abfangen. **Stand-Korrektur (2026-07-31):** In den Modell-Definitionen existiert nur EIN Unique-Index – `guestInformations(FestivalEventId, UserId)`. Die v0.7.15 für `groupMembers`/`friendships` festgestellten Indizes stehen allenfalls in der Prod-DB (Relikt der `alter: true`-Ära), nicht im Code – frische DBs (Dev/E2E/neu aufgebaute Prod) haben sie nicht, und der Kommentar in `friendship.service.ts` („Der Unique-Index deckt nur …") verweist auf einen Index, den es im Modell gar nicht gibt. **Achtung:** Bei bereits befüllter Prod-DB greift `sync()` nicht mehr → hängt am [L]-Punkt „Schema-Stabilität/Migrationen". _(Sicherheit)_
- [x] 🔴 **Session-Cookie härten (opakes Token statt JSON-Identität):** _Erledigt (2026-07-31): Der Cookie enthält nur noch den opaken Zufalls-Token (`crypto.randomUUID()`). Der Auth-Hook löst ihn per `UserService.getCurrentUserBySessionToken` auf (SessionToken-Lookup per Token, Ablaufprüfung inkl. DB-Cleanup, User-Load) und legt die Identität in `locals.currentUser` – ALLE Endpoints/Loader lesen jetzt `locals.currentUser` statt `UserService.extractUser(cookies)` (deckt sich mit der CLAUDE.md-Regel). `SessionTokenUser` + `resolveSessionToken` entfernt; `createSession` erzeugt bei Login/Registrierung/Passwortänderung immer einen frischen Token; Cookie wird nicht mehr pro Request neu geschrieben; das Profil-Update prüft `nicknameChanged` jetzt gegen den DB-Nickname (Bypass geschlossen). Verifiziert: svelte-check, Lint, 34 Unit-Tests, 49 E2E-Tests grün (inkl. Cookie-Manipulations-Test)._ Ursprünglicher Befund: Der Cookie ist ein client-manipulierbarer JSON-Blob `{id, token, nickname}`. `validateSessionToken` bindet nur `token` an `id`; der `nickname` wird nie gegen die DB geprüft, aber überall vertraut – u. a. für die Passwort-Prüfung in `/settings` (`loginWithCredentials(user.nickname, …)`) und die `nicknameChanged`-Logik im Profil-Update. Ein Nutzer kann seinen eigenen Cookie editieren und so z. B. die Nickname-Eindeutigkeitsprüfung umgehen (siehe voriger Punkt). Fix: Im Cookie nur ein opakes Zufalls-Token speichern; `id`/`nickname` bei der Validierung aus der DB laden und in `locals.currentUser` legen – und alle Endpoints von `UserService.extractUser(cookies)` auf `locals.currentUser` umstellen (deckt sich mit der CLAUDE.md-Regel). Überschneidet sich mit dem [M]-Punkt „Hook – JWT/HMAC": beide zusammen entscheiden (stateful opak vs. signiert stateless), nicht getrennt umsetzen. _(Sicherheit)_
- [ ] 🟡 **CI-Smoke-Test gegen den Prod-Build (adapter-node):** Die E2E-Suite läuft gegen `vite dev` – Verhalten, das nur der adapter-node-Build zeigt, wird nirgends getestet. Genau so blieb das `BODY_SIZE_LIMIT`-Problem unentdeckt (Upload-Limit existiert im Dev-Server nicht) und wurde erst im Review 2026-07-31 gefunden; gleiche Lücken-Klasse wie SQLite-vs-MariaDB. Fix: neuer Job in `tests.yml`: `npm run build`, dann `PLAYWRIGHT=true BODY_SIZE_LIMIT=2M PORT=5173 node build` starten (In-Memory-SQLite, kein MariaDB nötig) und wenige Smoke-Checks dagegen fahren – Registrierung/Login, Startseite, **Avatar-Upload mit ~1-MB-Bild** (verifiziert das Limit), ein API-Endpoint. Kann als kleines Playwright-Projekt mit eigenem `webServer`-Command umgesetzt werden. Überschneidet sich mit Schritt 5 des [L]-Migrationsplans (MariaDB-Job als Drift-Schutz) – beim Umsetzen zusammen denken, ersetzt ihn aber nicht. _(Testen)_
- [ ] 🟡 **CI-Job: Migrationen gegen echte MariaDB (Plan-Schritt 5):** Der SQLite-Drift-Test (`migrations.spec.ts`, v0.7.24) sichert Modelle ↔ Migrationen strukturell ab, aber dialektfrei – MariaDB-spezifisches Migrations-SQL bleibt ungetestet (die einzige automatisierungsfreie Lücke, siehe CLAUDE.md §1). Fix: Job in `tests.yml` mit MariaDB-Service-Container: Migrationen via App-Start gegen leere MariaDB hochfahren, Smoke-Test (Registrierung, Festival anlegen, Kommentar). Idealerweise mit dem Punkt „CI-Smoke-Test gegen den Prod-Build" (oben) zu EINEM Job kombinieren: Prod-Build + MariaDB = maximale Prod-Nähe in der CI. _(Datenbank)_
- [ ] 🟡 **Feldlängen: VARCHAR(255)-Grenzen existieren nur in Prod:** `comment`, `description`, `location` usw. sind `DataTypes.STRING` = VARCHAR(255) in MariaDB. SQLite (Dev/Tests) erzwingt das nicht – ein 500-Zeichen-Kommentar funktioniert lokal und wirft in Prod einen unbehandelten Sequelize-Fehler → 500. Serverseitige Längen-Validierung ergänzen (Konstanten + Prüfung in den Services, unit-testbar) und für echten Freitext (`comment`, `description`) auf `DataTypes.TEXT` wechseln (Schemaänderung → Migrations-Thema, siehe [L]). Genau die MariaDB/SQLite-Drift-Klasse, die CLAUDE.md §1 als ungetestet benennt. _(Datenbank)_

## [L] Groß (> 4h)

- [ ] 🟡 **Passwort-Management:** Definition von Passwort-Richtlinien (Komplexität) und Implementierung eines sicheren Passwort-Wiederherstellungsprozesses. _(Sicherheit)_
- [ ] 🟡 **Datenschutz:** Datenschutzhinweise prüfen, Löschkonzept für Accounts und Daten erstellen. Beim Account-Löschen zusätzlich die **Profil-Kommentare** (`comments.writtenTo` = User-ID) explizit mitlöschen – `writtenTo` ist polymorph und hat daher keinen FK/Cascade (analog zu `deleteFestival`, v0.7.15). _(Datenschutz)_
- [ ] 🟡 **Sichtbarkeit:** Festivals nur für Freunde oder Mitglieder von Gruppen zugänglich machen. _Stand (2026-07-31): teilweise umgesetzt – `GET /user/:id/visiting-festivals` ist bereits auf Self/Freunde beschränkt (E2E-getestet in `authorization.spec`). Offen ist die globale Festival-Liste auf der Startseite: `getAllFestivals` liefert weiterhin ALLE Festivals inkl. Gästeliste an jeden eingeloggten Nutzer._ _(Festivals)_
- [x] 🟡 **Schema-Stabilität – Migrationen mit umzug:** ~~Echte Migrationsstrategie einführen.~~ _Erledigt (v0.7.24), Schritte 1–4, 6, 7 des Plans: `startDB()` verzweigt nach Dialekt (MariaDB → `umzug.up()` mit `SequelizeStorage`/`SequelizeMeta`, SQLite → `sync()`); Migrationen als Modul-Array via `import.meta.glob` (Runner `src/lib/db/migrations.ts`, bundlefest); Baseline `0001-initial-schema.ts` EINGEFROREN und von Hand ausgeschrieben statt aus `sync()` abgeleitet – bewusste Abweichung vom Plan: eine sync-basierte Baseline würde mit den Modellen mitdriften und auf frischen DBs mit Folge-Migrationen kollidieren. Absicherung stattdessen per neuem Drift-Test `migrations.spec.ts` (baut beide Pfade gegen frisches SQLite und vergleicht Tabellen/Spalten/Unique-Constraints strukturell). CLAUDE.md: Triad → Quadrat-Regel, Abschnitt „Initialisierung" neu. Fail-fast in `startDB()` (eigener [S]-Punkt, miterledigt). **Prod-Cutover (einmalig, vor dem nächsten Deploy):** Die vorhandenen Prod-Tabellen enthalten nur verzichtbare Testdaten → auf dem Host alle Tabellen der App-DB droppen (z. B. `DROP DATABASE <user>_<name>; CREATE DATABASE <user>_<name>;`). Beim ersten Start legt die Baseline-Migration dann das komplette Schema inkl. der neuen Unique-Indizes an; ohne den Drop schlägt `createTable` auf die Alt-Tabellen fehl und der Server startet nicht (fail-fast, gewollt)._ **Offen bleibt Schritt 5** – als eigener Punkt unter [M] erfasst. Ursprünglicher Plan: _Teilentschärft (v0.7.9): `sync({ alter: true })` → schlichtes `sync()` (kein automatisches Schema-Alter mehr, damit kein heuristischer Datenverlust in Prod). Offen bleibt der eigentliche Punkt: Sobald die Prod-DB befüllt ist, werden Modelländerungen NICHT mehr automatisch übernommen – die App startet normal und fällt erst zur Laufzeit mit „Unknown column" um._ **Kipppunkt:** der erste echte Datenbestand. Drei offene Tasks sind bereits Schemaänderungen an befüllten Tabellen (Teilnehmer-Status „Vielleicht" → neue Spalte/Enum in `guestInformations`; Bilder aus der DB → Spaltenwechsel; Multiple Sitzungen → PK-Wechsel in `sessionTokens`). Vor dem ersten produktiven Datenbestand angehen, danach kostet es zusätzlich eine Bestandsaufnahme des Ist-Schemas. **Plan (Aufwand 4–6 h):**
  1. **Kernentscheidung:** Migrationen nur für MariaDB (Prod); Dev/E2E bleiben auf `sync()` gegen frisches In-Memory-SQLite (schnell, dialektfrei). `startDB()` verzweigt nach Dialekt. Preis: Schema-Drift zwischen Modellen (Quelle für `sync()`) und Migrationen (Quelle für Prod) → Punkt 4 ist deshalb Pflicht, nicht optional.
  2. **Migrationen als Modul-Array, nicht als Dateisystem-Ordner:** Der Standard-Weg (`migrations: { glob: 'migrations/*.js' }`) liest zur Laufzeit vom Dateisystem und bricht hier, weil `vite build` einen gebündelten Node-Server nach `build/` wirft (kein `migrations/`-Ordner, wird auch nicht gersynct). Stattdessen per `import.meta.glob('./migrations/*.ts', { eager: true })` einsammeln, nach Dateinamen sortieren und umzug als `[{ name, up, down }]`-Array übergeben → landet im Bundle, kein Extra-Deploy-Schritt, keine CLI auf dem Host.
  3. **umzug in `startDB()`** mit `SequelizeStorage` (Tabelle `SequelizeMeta`). Bei MariaDB `umzug.up()` statt `sync()`; führt nur Pending-Migrationen aus, also bei jedem Start idempotent. `startDB()` wird bereits auf Modulebene in `hooks.server.ts` awaited → Migrationen laufen vor dem ersten Request.
  4. **Baseline-Migration `0001-initial-schema`** (Tabellen, FKs mit `ON DELETE CASCADE`, Unique-Indizes), generiert aus dem tatsächlichen `sync()`-Ergebnis statt von Hand. Prod-DB noch leer → droppen und Baseline anlegen lassen. Prod-DB bereits befüllt → Baseline einmalig als „ausgeführt" in `SequelizeMeta` eintragen, ohne sie laufen zu lassen. **Vorher klären, welcher Fall zutrifft.**
  5. **CI-Job als Drift-Schutz** (der Teil, der die Strategie überhaupt trägt): neuer Job in `tests.yml` mit MariaDB-Service-Container → Migrationen gegen leere MariaDB hochfahren, App starten, Smoke-Test (Registrierung, Festival anlegen, Kommentar). Damit fliegen fehlende Spalten und kaputtes Migrations-SQL vor dem Deploy auf.
  6. **Triad Rule → Quadrat:** CLAUDE.md-Regel um „Migration schreiben" ergänzen, sonst ist die Strategie nach drei Wochen wieder tot.
  7. **Aufräumen:** `sequelize-cli` aus `dependencies` entfernen (unbenutzt, von umzug nicht gebraucht, landet über `npm ci --omit=dev` unnötig auf dem Prod-Host).

  **Risiken:** Migrationslauf beim Serverstart ist bei EINEM Node-Prozess unkritisch; bei mehreren Instanzen bräuchte es ein Advisory Lock (als Kommentar im Code festhalten). Eine fehlgeschlagene Migration verhindert künftig den Serverstart – beabsichtigt (besser als stillschweigend falsches Schema), heißt aber: Deploys können härter fehlschlagen als heute. _(Datenbank)_

- [ ] 🟢 **Multiple Sitzungen:** Unterstützung für gleichzeitige Logins auf mehreren Geräten bei gleichzeitigem Schutz vor ungewollten Sitzungen. _(Sicherheit)_
- [ ] 🟢 **E2E-Tests:** Abdeckung von Login/Logout, Registrierung, Event-Teilnahme und Berechtigungen erweitern. _Stand (2026-07-31): im Kern erledigt – `auth.spec` (Registrierung/Login/Logout inkl. Fehlerfälle), `authentication.spec` (Cookie-Manipulation, Lockout), `festival.spec` (kompletter Zu-/Absage-Lifecycle), `authorization.spec` (Fremd-Löschen verboten, Freunde-Sichtbarkeit). Punkt kann geschlossen oder auf konkrete Lücken (z. B. Kommentar-Berechtigungen via API) eingegrenzt werden._ _(Testen)_
- [ ] 🟢 **Bilder in der Datenbank:** User-Bilder werden als Base64-String direkt in der DB gespeichert (`src/lib/services/user.service.ts`). Bei vielen Nutzern belastet das die DB erheblich. Fix: Bilder im Dateisystem oder einem Object Storage ablegen und nur den Pfad in der DB speichern. _(Performance)_
